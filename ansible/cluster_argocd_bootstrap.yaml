- name: Deploy ArgoCD to the cluster
  hosts: control_plane_management, workers_management
  gather_facts: true
  roles:
    - name: ArgoCD prerequisites
      role: argocd_prerequisites
      tags: [argocd_prereq]

    - name: Deply Argocd to cluster
      role: deploy_cluster_addons
      tags: [argocd_addons]
     